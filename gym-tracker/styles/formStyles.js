import { StyleSheet } from "react-native";

const formStyles = StyleSheet.create({
    setModifierOff: {
        borderColor: 'red',
        borderWidth: 1
    },
    setModifierOn: {
        borderColor: 'green',
        borderWidth: 1
    }
});

export default formStyles;